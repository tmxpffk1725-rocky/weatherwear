# 오라클 VM의 /etc/weatherwear-backend.env 로 복사해 채울 것 (커밋 금지)
# JWT 서명 비밀키 — 길고 무작위로. 예: openssl rand -hex 32
JWT_SECRET=

# 프론트 출처 허용(쉼표 구분). Vercel 도메인 + 로컬 개발
FRONTEND_ORIGINS=https://weatherwear-jade.vercel.app,http://localhost:5173

# 포트(기본 3000) / DB 경로(기본 ./data/weatherwear.db)
PORT=3000
